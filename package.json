{
  "name": "grid-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An isometric, zoomable, explorable model of the California power grid.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "three": "^0.170.0"
  }
}
